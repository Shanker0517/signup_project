import { NgbDateStruct } from "@ng-bootstrap/ng-bootstrap";

export interface Userinterface {
    firstName: string;
    lastName: string;
    email: string;
    dateofbirth:Date;
    phoneno:number;
    password:string;
}
